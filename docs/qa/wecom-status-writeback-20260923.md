# 企微智能表状态回写验收记录

日期：2026-09-23

## 本次修改

- 新增独立状态回写配置与服务端字段白名单，默认关闭，不修改数据库表结构。
- 从 RegistrationAttendee、Registration、Order、Refund 读取持久化事实；不修改支付、退款、签到业务事务。
- 在现有手动和定时同步任务中更新已绑定行的指定文本列，保留其他单元格；按行比对实现幂等与失败重试。
- 包含身份一致性、双侧唯一性、重复绑定、字段冲突/类型检查、同一子表多会议写入保护。
- 后台增加列选择和同步问题明细，并修复连接抽屉的窄屏内部溢出。

## 已通过

| 验证 | 结果 |
| --- | --- |
| pnpm test | PASS：共享包 13、API 487、用户端 66，共 566 项；无失败、无跳过 |
| pnpm typecheck | PASS：全 workspace 类型检查 |
| pnpm build:api | PASS |
| pnpm build:admin | PASS；保留既有大 chunk 提示 |
| 后台权限检查脚本 | PASS：316 个控制器权限引用 |
| guest-schedule-smart-sheet.spec.ts | PASS：3 个浏览器用例，固定截图基准复跑通过 |
| 桌面 1440 / 手机 390 宽度 | PASS：字段选择、保存请求、公式列排除、抽屉及表单边界检查；截图人工复核 |
| git diff --check | PASS |

专项覆盖：关闭时不写、仅更新指定列、重复同步、撤销签到、取消/退款后继续同步、部分退款、免付费、多参会人同单、手机号/ID 冲突、行丢失、列冲突、失败重试、响应丢失后幂等、旧同步记录兼容、事项读取失败不阻止独立状态阶段。

## 未执行与上线步骤

- NOT_RUN：真实企微文档写权限验证、真实字段清空行为、现场手机扫码到智能表的端到端联调。
- NOT_RUN：生产部署与生产配置启用；未修改生产表格、用户、订单、支付或退款数据。
- 本地预览使用合成数据，不调用企微、不读取生产信息，不能当作真实回写验收。
- 部署只需 API 与后台，不需要修改小程序包；随后按配置文档选择专用文本列、检查连接、以一位测试嘉宾完成扫码/撤销扫码联调。
- 回滚：关闭“系统状态回写”或停用连接。已回写的单元格不会自动还原；开启前应保留表格历史或备份。
