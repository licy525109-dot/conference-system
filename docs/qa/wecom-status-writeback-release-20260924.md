# 企微智能表状态回写发布检查

日期：2026-09-24

## 发布范围

- 发布 API 状态回写逻辑、后台配置和同步明细；不修改小程序包。
- 无数据库结构变更，不修改订单、支付、退款和签到业务事务。
- 状态回写默认关闭，保留生产连接原有字段映射和同步开关。
- 不上传本地预览服务、合成数据或无关输出文件。

## 发布前验证

| 检查 | 结果 |
| --- | --- |
| 全量单元测试 | PASS：566 项，0 失败、0 跳过 |
| workspace 类型检查与 lint | PASS |
| API / Admin 构建 | PASS |
| 后台权限检查 | PASS：316 个控制器权限引用 |
| 智能表浏览器回归 | PASS：3 项，含桌面和 390px 手机宽度 |
| git diff --check | PASS |
| 上线前生产健康检查 | PASS：API ready、数据库 ok |

## 发布与验收边界

- 合并前须通过 GitHub Pull Request Quality；合并后使用 Deploy Baota Production 自动备份并部署。
- 部署完成后核对生产提交、PM2 状态、公网健康、静态文件及连接配置摘要。
- 发布前以上生产部署与发布后检查尚待执行，最终结果以对应 Actions 记录和本次任务交付为准。
- 真实企微编辑权限、文本清空语义、手机扫码签到与撤销签到端到端仍须使用已确认的测试嘉宾验收。
- 启用前选择专用文本列，执行连接检查；不将“部署成功”等同于“已启用回写”。

配置操作与回滚边界见 [状态回写配置说明](../integrations/WECOM_STATUS_WRITEBACK.md)。历史本地验证见 [2026-09-23 验收记录](wecom-status-writeback-20260923.md)。
