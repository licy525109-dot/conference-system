{
  "name": "conference-system",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@11.5.2",
  "scripts": {
    "dev:api": "PORT=${PORT:-3001} pnpm --filter @conference/api dev",
    "dev:user:h5": "pnpm --filter @conference/user dev:h5",
    "dev:user:mp-weixin": "pnpm --filter @conference/user dev:mp-weixin",
    "dev:admin": "pnpm --filter @conference/admin dev",
    "build:api": "pnpm --filter @conference/api build",
    "build:user:h5": "pnpm --filter @conference/user build:h5",
    "build:user:mp-weixin": "pnpm --filter @conference/user build:mp-weixin",
    "build:admin": "pnpm --filter @conference/admin build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "prisma:generate": "mkdir -p .cache .tmp && XDG_CACHE_HOME=$PWD/.cache TMPDIR=$PWD/.tmp dotenv -e .env.local -- pnpm --filter @conference/api prisma:generate",
    "prisma:migrate": "mkdir -p .cache .tmp && XDG_CACHE_HOME=$PWD/.cache TMPDIR=$PWD/.tmp dotenv -e .env.local -- pnpm --filter @conference/api prisma:migrate",
    "prisma:studio": "mkdir -p .cache .tmp && XDG_CACHE_HOME=$PWD/.cache TMPDIR=$PWD/.tmp dotenv -e .env.local -- pnpm --filter @conference/api prisma:studio",
    "prisma:seed": "pnpm seed",
    "seed": "mkdir -p .cache .tmp && XDG_CACHE_HOME=$PWD/.cache TMPDIR=$PWD/.tmp dotenv -e .env.local -- pnpm --filter @conference/api seed"
  },
  "devDependencies": {
    "@prisma/client": "^6.19.3",
    "dotenv-cli": "^8.0.0",
    "tsx": "^4.20.6"
  }
}
